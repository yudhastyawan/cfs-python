% 'Input',...
hresult{1} = [...
    'This menu is used for either opening preexisting in',...
    'put file or buliding a new preexisting input file o',...
    'r buliding a new input file using submenues'];

% 'Input menu',... 
hresult{2} = [...
    'This menu is used for either opening preexisting in',...
    'put file or buliding a new preexisting input file o',...
    'r buliding a new input file using submenues'];

% 'Functions',... 
hresult{3} = [...
    'This menu is used for calculating and rendering deformation fiel',...
    'd and stress changes based on the information from any input file r',...
    'ead already from Input menu. Also, submenu tools provide user a fe',...
    'w tools to taper or split each fault and to coordinate principal s',...
    'tress axes, and to convert lon & lat info into Cartesian coordinates.'];

% 'Functions menu',... 
hresult{4} = [...
    'This menu is used for calculating and rendering deformation fiel',...
    'd and stress changes based on the information from any input file r',...
    'ead already from Input menu. Also, submenu tools provide user a fe',...
    'w tools to taper or split each fault and to coordinate principal s',...
    'tress axes, and to convert lon & lat info into Cartesian coordinates.'];

% 'Overlay',... 
hresult{5} = [...
    'One of the main menus. This allows user to overlay mapped informatio',...
    'such as coastline, mapped active faults, earthquakes, text marks. Yo',...
    'u can also use "Trace and put faults into input" submenu to trace othe',...
    'r faults easily on the graphic window. To overlay them, you need well-f',...
    'ormatted data to be read properly. See the formats in this help menu se',...
    'arching each submenu.'];

% 'Overlay menu',... 
hresult{6} = [...
    'One of the main menus. This allows user to overlay mapped informatio',...
    'such as coastline, mapped active faults, earthquakes, text marks. Yo',...
    'u can also use "Trace and put faults into input" submenu to trace othe',...
    'r faults easily on the graphic window. To overlay them, you need well-f',...
    'ormatted data to be read properly. See the formats in this help menu se',...
    'arching each submenu.'];

% 'About Coulomb PC',... 
hresult{7} = [...
    'It shows version number, authors, and credit.'];

% 'Open existing input file',... 
hresult{8} = [...
    'One of the submenus under menu "Input." It firstly opens OS dialog to sel',...
    'ect one of the input files you want to calculate. Then the software read',...
    'all parameters in the file without confirming each parameter. User can mak',...
    'e sure the name and directory of the file on the MATLAB command window. I',...
    'f the file does not have any information about lon. and lat. for their pos',...
    'sitions, the message "No lat. & lon. information is included in the input f',...
    'ile" appears. But do not mind.'];

% 'Open & edit input file',... 
hresult{9} = [...
    'One of the submenus under menu "Input." It firstly opens OS dialog to sel',...
    'ect one of the input files you want to calculate. Then the software read',...
    'all parameters in the file and show a dialog to confirm or edit the para',...
    'meters. User can change them in this process and can save the modified par',...
    'ameters pushing "Save as" button. User can also make sure the name and di',...
    'rectory of the file on the MATLAB command window. I',...
    'f the file does not have any information about lon. and lat. for their pos',...
    'sitions, the message "No lat. & lon. information is included in the input f',...
    'ile" appears. But do not mind.'];

% 'Build input file from x & y map',...
hresult{10} = [...
    'You can build an input file in Cartesian coordinates by choosing Input > Bui',...
    'ld input file from x & y map. After you approve the study area dimension, yo',...
    'u can use mouse click to position a fault using left- and right-button click',...
    's. To save the fault and study area, you can choose either ''Save input fil',...
    'e as ascii'' or ''Save input+overlays as binary''.'];

% 'Build input file from lon. & lat. map',...
hresult{11} = [...
    'You can build an input file in lat/lon coordinates by choosing Input > Bui',...
    'ld input file from lon. & lat. map. The map posistion to Cartesian position',...
    's calculator will apprear.'];

% 'Save input file as ascii',... 
hresult{12} = [...
    'One of the submenus under menu "Input." User can save the parameters intera',...
    'ctively changed during a session as a ascii-formatted file which can be edi',...
    't in any text editor.'];

% 'Save input+overlays as binary',...
hresult{13} = [...
    'One of the submenus under menu "Input." User can save the parameters intera',...
    'ctively changed during a session as a binary file (.mat). The advantage of thi',...
    's function is that user can include all additional data such as coastlines, mapp',...
    'ed faults, and earthquake plots. In addition, the file is relatively light and fas',...
    't for reading operation. User can edit the parameters using "Input" > "Open & e',...
    'dit input file" when reading.'];

% 'Add coords to input file',... 
hresult{14} = [...
    'One of the submenus under menu "Input." It enables user to add lon. & lat. in t',...
    'he parameters read from the input file. This process then allows user to overla',...
    'y coastlines, mapped faults, etc. To keep the information, user needs to save th',...
    'e parameters using "Input" > "Save input file as ascii" or "Save input+overlay a',...
    's binary.'];

% 'Preferences',... 
hresult{15} = [...
    'One of the submenus under menu "Input." It opens the preference panel to chang',...
    'e users own setting (color and width of grid lines, fault lines, etc.). The p',...
    'rogram automatically saves the setting when quiting coulomb 3.0. The settin',...
    'g info is saved in "preferences.dat" file in "preferences" folder.'];

% 'Grid',... 
hresult{16} = [...
    'One of the submenus under menu "Functions." It shows either a simple map view of grid',...
    'ded study area with fault mapview projection (2D map view) or an oblique view of 3D i',...
    'image of grid and faults (3D view).'];

% '2D map view',...
hresult{17} = [...
    'One of the submenus under menu "Functions" > "Grid". It shows a simple map view of grid',...
    'ded study area with fault mapview projection. Note that rectangular edges of fau',...
    'lts is projected as red-colored lines as default. A green line indicates the surf',...
    'ace projection of the fault upper edge, whereas a black line is an intersection b',...
    'etween the fault and calculation depth. To save this image, you can select "Fil',...
    'e" > "Save As" and name the image in OS dialog. [Warning: do not overwrite "mai',...
    'n_menu_window.fig." Change the name and file format as you like]'];

% '3D view',... 
hresult{18} = [...
    'One of the submenus under menu "Functions" > "Grid". This submenu produces a obli',...
    'que view of 3D image of entire gridded study area and faults which are color-code',...
    'd depending on their slip amounts. User can rotate the 3D image using "Tool',...
    's" > "3D rotate" or one of the tool bar icons (circled arrow). The slip components o',...
    'f the faults can be easily switched clicking radiobutton in a panel next to the mai',...
    'window.'];

% 'Displacements',...
hresult{19} = [...
    'Displacements can be expressed as vectors or distorted grid on a ma',...
    'p and 3D view. After reading an input file, choose Functions > Displa',...
    'cement > ...'];

% 'Horizontal displ. (vectors)',... 
hresult{20} = [...
    'A submenu under "Funtions" > "Displacements". This enables user to se',...
    'e displacement vectors moved from each node on map view. The amount o',...
    'f displacement in the graphic can be controlled by either the value i',...
    'n the input file (named "Exaggeration for disp.& dist.=") or slider o',...
    'n the contol panel appeared together. The default setting is for displ',...
    'acement without any fixing point. User can fix the referecen point b',...
    'y either typing x and y textfield on the control panel or clicking a p',...
    'osition by mouse on the main window after push "mouse click" button o',...
    'n the control panel. The numerical output file is au',...
    'tomatically saved as "Displacement.cou" in "output_files" folder.'];

% 'Vertical displ. (wireframe)',... 
hresult{21} = [...
    'editing'];

% '3D image drape',... 
hresult{22} = [...
    '3D image drape editing.'];

% '3D wireframed surface',... 
hresult{23} = [...
    '3D wireframed surface editing.'];

% 'Strain',... 
hresult{24} = [...
    'Strain editing.'];

% 'Stress',... 
hresult{25} = [...
    'Stress editing.'];

% 'Shear stress change',...
hresult{26} = [...
    'A submenu under "Funtions" > "Stress". It directly allows us to see shea',...
    'r stress change on assumed receiver fault planes on grid nodes. Also se',...
    'e coulomb stress change for detail.'];

% 'Normal stress change',... 
hresult{27} = [...
    'A submenu under Funtions > Stress. It directly allows us to see norma',...
    'l stress change on assumed receiver fault planes on grid nodes. Also se',...
    'e coulomb stress change for detail.'];

% 'Coulomb stress change',...
hresult{28} = [...
    'Coulomb stress change editing.'];

% 'Calc. stress on faults',... 
hresult{29} = [...
    'A submenu under Funtions > Stress. This enables us to calculate Coulom',...
    'b stress change on a center of each fault element. The results are show',...
    'in Coulomb 3.0 main graphic window as a 3D view. The numerical output fi',...
    'le named "Element_test.csv" is saved in output_files folder in home dir',...
    'ectory "coulomb30".'];

% 'Calc. stress on a point',...
hresult{30} = [...
    'Calc. stress on a point editing.'];

% 'Calc. stress on nodal plane',... 
hresult{31} = [...
    'Calc. stress on nodal plane editing.'];

% 'Change parameters',... 
hresult{32} = [...
    'A submenu under menu Functions. It can allow user to change most of f',...
    'ault parameters and graphic parameters interactively. You do not hav',...
    'e to edit them on the text editor.'];

% 'All input parameters',... 
hresult{33} = [...
    'All input parameters editing.'];

% 'Grid size',... 
hresult{34} = [...
    'A submenu under menu "Functions" > "Change paramters". This enables u',...
    's to change increments interactively for both x and y axes, in other wo',...
    'rds mesh size. Note that it wont overwrite the input file unless you sav',...
    'e the change using "Input" > "Save Inputfile as ascii" or "Save Input+ov',...
    'erlays as binary".'];

% 'Calculation depth',... 
hresult{35} = [...
    'A submenu under menu "Functions" > "Change paramters". This enables u',...
    's to change the calculation depth. Note that it wont overwrite the inp',...
    'ut file unless you save the change using "Input" > "Save Inputfile a',...
    's ascii" or "Save Input+overlays as binary".'];

% 'Coeff. friction',... 
hresult{36} = [...
    'A submenu under menu "Functions" > "Change paramters". This enables u',...
    's to change the coefficient of friction interactively. Note that it won',...
    't overwrite the input file unless you save the change using "Input" > "S',...
    'ave Inputfile as ascii" or "Save Input+overlays as binary".'];

% 'Vertical exaggeration',... 
hresult{37} = [...
    'A submenu under menu "Functions" > "Change paramters". This enables u',...
    's to change the vertical exaggeration for functions "Functions" > "Dis',...
    'placement" > "3D image draped" and "3D image wireframed surface" inte',...
    'ractively. Note that it won',...
    't overwrite the input file unless you save the change using "Input" > "S',...
    'ave Inputfile as ascii" or "Save Input+overlays as binary".'];

% 'Tools'...
hresult{38} = [...
    'Tools editing.'];

% 'quit coulomb',...
hresult{39} = [...
    'To close the Coulomb 3.0 window, click Input > Quit.'];

% 'lauch coulomb',...
hresult{40} = [...
    'In the MATLAB menu bar top line, set the path of Coulom',...
    'b folder, and then type coulomb in the Command Window.la',...
    'unch coulomb editing.'];

% 'input file (open)'...
hresult{41} = [...
    'input file (open) editing.'];

% 'Horizontal displ. (wireframe)',... 
hresult{42} = [...
    'A submenu under "Funtions" > "Displacements". This enables user to se',...
    'e exaggerated distorted grid on map view. The amount o',...
    'f distortion in the graphic can be controlled by either the value i',...
    'n the input file (named "Exaggeration for disp.& dist.=") or slider o',...
    'n the contol panel appeared together. The numerical output file is au',...
    'tomatically saved as "Displacement.cou" in "output_files" folder.'];

% 'friction',...
hresult{43} = [...
    'apparent friction coefficient used for coulomb stress calculation.',...
    'This is dimensionless (0-1) but 0.4 is often used. You can change',...
    'the number anytime from ''Functions''>''Change paramters''>',...
    '''Coeff. friction''. In the workspace, it is named FRIC'];

% 'regional stress',...
hresult{44} = [...
    'regional stress editing.'];

% 'poisson ratio',...
hresult{45} = [...
    'Poisson''s ratio is the ratio of transverse strain devided by the',...
    'relative extension strain. Dimensionless ranging 0 to 1. 0.25 is',...
    'normally used. In the workspace, it is named POIS'];

% 'young modulus',...
hresult{46} = [...
    'Young''s modulus is a measure of the stiffness of a given material',...
    '(Wikipedia). Unit in this program is bars. 8x10^5 bars is typically',...
    'used. In the workspace, it is named YOUNG'];

% 'source fault',...
hresult{47} = [...
    'source fault has slip and impart stress. But receivers do not.'];

% 'receiver fault'...
hresult{48} = [...
    'Receiver fault does not have any slip and receive stress.'];

% 'opt. faults',...
hresult{49} = [...
    'opt. faults editing.'];

% 'opt. strike-slip faults',...
hresult{50} = [...
    'opt. strike-slip faults editing.'];

% 'opt. thrust faults',...
hresult{51} = [...
    'opt. thrust faults editing.'];

% 'opt. normal faults',...
hresult{52} = [...
    'opt. normal faults editing.'];

% 'specified fault',...
hresult{53} = [...
    'specified fault editing.'];

% 'strike line',...
hresult{54} = [...
    'strike line editing.'];

% 'strike',...
hresult{55} = [...
    'strike editing.'];

% 'dip',...
hresult{56} = [...
    'dip editing.'];

% 'rake',...
hresult{57} = [...
    'rake editing.'];

% 'aki and richards convention'
hresult{58} = [...
    'aki and richards convention editing.'];

% 'plug-ins',...
hresult{59} = [...
    'plug-ins collects additional functions and script useful for furthe',...
    'r plotting and calculations. All function and script files are in pl',...
    'ug-ins folder. You can simply type the file name without .m on the c',...
    'ommand window to execute the script.'];

% 'coulomb2googleearth',...
hresult{60} = strvcat(...
    'Google Earth offers wonderful opportunities to drape Coulomb',...
    'files on satellite images and DEMs. You need to download and',...
    'install Google Earthon your computer before following these ',...
    'steps. In addition to this, you will need Adobe Illustrator',...
    'or any reasonable graphic application.',...
    ' ',...
    '1. Make sure that you have selected the lon. & lat. Coordi-',...
    '   nate option in the Preferences menu.',...
    '2. Run any horizontal displacement function, such as Horizontal',...
    '   displ.(vectors) or Horizontal displ. (wireframe). Add the ',...
    '   Overlay options that you would like in the plot. When the ',...
    '   final plot appears in the Coulomb 3.0 window, type ',...
    '   ''coulomb2googleearth'' in the MATLAB command window, and ',...
    '   hit return. You will get messages in the command window ',...
    '   once files are saved. Then, you can drag the kml files ',...
    '   onto the Google Earth icon on your computer dock or desktop,',...
    '   or you can open the files from the Google  Earth menu.',...
    ' ',...
    'This file was made using the input file, ''Landers-uniform-',...
    'slip.inp'' and Displacement > Horizontal Displ. (Vectors).',...
    ' ',...
    '3. For color gradient images such as are created in the ',...
    '   Coulomb stress change, Shear stress change, Normal stress',...
    '   change options, or the Vertical displacement (color) ',...
    '   options File > Save As... pdf using the commands,',...
    '   renaming them.',...
    '4. Open the .pdf files in Illustrator or the graphics ',...
    '   software you are using, and remove all the text and lines,',...
    '   and keep only the color (raster) image. Save the color ',...
    '   image as .jpeg file, and name it ''coulombmap.jpeg'' ',...
    '   in the Coulomb directory',...
    '5. Type ''coulomb2googleearth'' in the command window. ',...
    '   This calls a script that is run by Coulomb; this is not',...
    '   a feature of the normal pull-down menus.  You will get',...
    '   the overlay kml file.');

% 'digitize_polygon',...
hresult{61} = [...
    'digitize_polygon editing'];

% 'kode',...
hresult{62} = strvcat(...
    'Kode controls calc. types using two elements. Normally 100 is used.',...
    'Kode ###:                ELEMENT(:,5)        & ELEMENT(:,6)',...
    'Kode 100: (rectanglar)   right-lat slip (m)  & reverse slip (m)',...
    'Kode 200: (rectanglar)   right-lat slip (m)  & tensile (m)',...
    'Kode 300: (rectanglar)   tensile (m)         & reverse slip (m)',...
    'Kode 400: (point source) right-lat slp(m^3)  & reverse slp(m^3)',...
    'Kode 500: (rect.& point) tensile (m^3)       & inflation (m^3)');

% 'unit',...
hresult{63} = strvcat(...
    'INPUT parameters and their dimensions (unit)',...
    ' Poisson''s ratio: [dimensionless, 0-1]; 0.25 is typically used',...
    ' Young''s modulus: [bars]; 8x10^5 bars is typically used',...
    ' Friction coefficient: [dimensionless; 0-1]; 0.4 is often used',...
    ' Directions, angle, and dip: [degrees; dip must be positive',...
    ' Grid and fault position (x,y): [km]',...
    ' Depth: [km] downward is positive',...
    ' Displacement: [km]',...
    ' Faults: right-lateral is positive, and reverse is positive [m]',...
    ' Dikes: Opening displacement is positive [m]',...
    ' Point Source: Inflation is positive [m^3]',...
    ' Regional stress tensor, S1, S2, S3: positive in compression [bars]',...
    ' ',...
    'OUTPUT parameters and their dimensions (unit)',...
    ' Displacement: [m] North, East, and Up are positive',...
    ' Shear Strain: [dimensionless] Right-lateral is positive',...
    ' Principal Strain: [dimensionless] Dilatation is positive',...
    ' Stress: [bars] Right-lateral and unclamping are positive');

% 'taper',...
hresult{64} = [...
    'To remove unrealistic stress concentrations at the edges of a fault,',...
    'you can taper the slip automatically in Coulomb. To do that, choo',...
    'se Functions > Tools > Taper on subdivide fault slip. When the Eleme',...
    'nt input panel appears, choose ''Linear tapering''. The tapered slip i',...
    's built by nested rectangles. For example, choose 10 km along strike, 5 k',...
    'm down-dip, and 5 nested patches, and hit OK. Save the tapered inpu',...
    't file by Input > Save input file as ascii with a new name, and op',...
    'en it in an editor to see the 5 nested coplaner sources.'];

% 'subdivide',...
hresult{65} = [...
    'subdivide editing'];

% 'principal axes calculator',...
hresult{66} = [...
    'principal axes calculator editing'];

% 'gps stations',...
hresult{67} = [...
    'gps stations editing'];

% 'annotations',...
hresult{68} = [...
    'annotations editing'];

% 'earthquake plot',...
hresult{69} = [...
    'earthquake plot editing'];

% 'active fault plot',...
hresult{70} = [...
    'active fault plot editing'];

% 'coastline plot',...
hresult{71} = [...
    'coastline plot editing'];

% 'global cmt',...
hresult{72} = strvcat(...
    'http://www.globalcmt.org/CMTfiles.html',...
    'Use ''ndk'' format.');

% 'japan meteorological agency (JMA)',...
hresult{73} = strvcat(...
    'There is no public downloadable site available.',...
    'Visit http://www.jma.go.jp/en/quake/ and send your request.');

% 'southern california eq center',...
hresult{74} = [...
    'http://www.data.scec.org/catalog_search/date_mag_loc.php'];

% 'northern california eq center',...
hresult{75} = [...
    'http://www.ncedc.org/ncedc/catalog-search.html'];

% 'usgs neic screen format',...
hresult{76} = [...
    'http://neic.usgs.gov/neis/epic/'];

% 'zmap formatted catalog',...
hresult{77} = strvcat(...
    'Format prepared for ZMAP software below.',...
    'http://www.earthquake.ethz.ch/software/zmap',...
    'space delimited 9 columns like below.',...
    '-----------------------------------------------------',...
    '(lon.)    (lat.) (yr) (mo) (dy) (M) (depth) (hr) (mn)',...
    '-116.430  34.207 1992  6 28 7.3   1.99 11 57',...
    '-116.398  34.135 1992  6 28 5.7   0.01 12  0',...
    '-116.533  34.547 1992  6 28 4.6  10.80 12 17');

% 'nied f-net catalog',...
hresult{78} = strvcat(...
    'http://www.fnet.bosai.go.jp/freesia/event/search/search.html',...
    'Use ''(space delimited) text format'' for output format.',...
    'And remove all header lines.');

% 'nied hi-net auto catalog',...
hresult{79} = strvcat(...
    'http://www.hinet.bosai.go.jp/ Only Japanese site available.',...
    'You need to register first, and then access auto catalog.');

% 'kandilli turkey catalog',...
hresult{80} = [...
    'kandilli turkey catalog editing'];

% 'hypodd/tomodd',...
hresult{81} = [...
    'hypodd/tomodd editing'];

% 'northern california dd',...
hresult{82} = [...
    'northern california dd editing'];

% 'sousus'
hresult{83} = [...
    'sousus editing'];

% 'input file building'
hresult{84} = strvcat(...
    'There are four ways to build an input file by yourself.',...
    '(1) Input > Open & edit input file (you can modify one of example files.)',...
    '(2) Input > Build input file from x & y map (mouse clicking in x&y coords)',...
    '(3) Input > Build input file from lon. & lat. map (with a large dialog box)',...
    '(4) Open one of the example files in text editor and modify numbers');


